apiVersion: v1
kind: ConfigMap
metadata:
  name: hermes-config
data:
  config.yaml: |
    settings:
      fingerprint_strategy: "auto"
      deduplication_ttl: 300
      metrics_port: 9090

    destinations: []
    groups: []
